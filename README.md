# ⚡ AMINCK Nova Bot

ربات حرفه‌ای فروش کانفیگ روی **Cloudflare Workers** — کاملاً سرورلس؛ مدیریت کامل **داخل خود تلگرام**.
دیتابیس داخل **Durable Object (SQLite)** است؛ یعنی دیپلوی با **یک کلیک** و بدون ساخت هیچ کلید KV/D1.

> 🤖 فروشگاه خودکار، پنل مدیریت شیشه‌ای، رفرال، مینی‌اپ سکه‌ای، چت هوش مصنوعی، QR برند، تایید فیش با هوش مصنوعی، تبلیغ گروه‌ها و یادآوری تمدید — همه در یک ورکر.

---

## 🚀 دیپلوی با یک کلیک

[![Deploy to Cloudflare Workers](https://deploy.workers.cloudflare.com/button)](https://deploy.workers.cloudflare.com/?url=https://github.com/amingangmanatgh2-hash/AMINCK-Nova-Edge)

1. روی دکمه بالا بزنید → به گیت‌هاب و کلادفلر وصل شوید → **Deploy**. در کانفیگ پروژه هیچ متغیر `[vars]` یا Secret نمونه‌ای تعریف نشده است، بنابراین فرم متغیرهای ربات و URL نشان داده نمی‌شود.
2. بعد از پایان دیپلوی، آدرس Worker را باز کنید و به `/setup` بروید.
3. توکن BotFather را در صفحه ستاپ وارد کنید. Worker ابتدا آن را با Telegram بررسی می‌کند، سپس وب‌هوک را روی `/webhook` ثبت و توکن را در SQLite داخل Durable Object ذخیره می‌کند.
4. در تلگرام `/start` بزنید — **اولین کاربر سوپرادمین** است و پروفایل بات (نام، توضیحات، دستورها، عکس) خودکار تنظیم می‌شود.

> ℹ️ آدرس Worker از اولین درخواست به‌صورت خودکار در Durable Object ذخیره می‌شود (لینک ساب، QR و مینی‌اپ بدون تنظیم URL اضافی کار می‌کنند).
> صفحه `/setup` فقط راه‌اندازی اولیه را انجام می‌دهد؛ توکن در HTML پاسخ یا لاگ پروژه چاپ نمی‌شود.

### دیپلوی دستی (اختیاری)

```bash
npm install
npx wrangler login
npm run setup     # = wrangler deploy
```

پس از دیپلوی دستی نیز آدرس Worker را باز کنید و `/setup` را اجرا کنید.

### توسعه محلی

برای توسعه محلی یک فایل نادیده‌گرفته‌شده به نام `.dev.vars` بسازید و مقدار توکن محلی را فقط در همان فایل قرار دهید؛ این فایل هرگز commit نمی‌شود:

```bash
touch .dev.vars
$EDITOR .dev.vars
npm run dev
```

---

## ✨ امکانات

| بخش | جزئیات |
|---|---|
| 👑 **سوپرادمین خودکار** | اولین `/start` = سوپرادمین؛ دکمه «📊 پنل مدیریت» فقط برای ادمین‌ها |
| 📊 **پنل مدیریت** | آمار روز/هفته/ماه، صف فیش‌ها با تایید/رد سریع، محصولات/سرورها/کاربران/ادمین‌ها با سطح دسترسی/گروه‌ها/تنظیمات/خروجی CSV و JSON — با دکمه‌های شیشه‌ای و ادیت پیام |
| 🛍 **فروشگاه** | VLESS / V2ray / Trojan / Shadowsocks / OpenVPN / MTProto / SOCKS5 + کانفیگ‌ساز اختصاصی (ویژه) |
| 💳 **پرداخت** | کارت‌به‌کارت + بررسی هوشمند فیش (بینایی Workers AI: مبلغ/تاریخ/ساعت/دستکاری) — مشکوک به صف ادمین، معتبر تایید خودکار؛ کیف پول و شارژ |
| 👥 **رفرال** | لینک `t.me/bot?start=ref_ID`، ۱۰٪ نقدی بعد اولین خرید زیرمجموعه، کانفیگ رایگان با ۵ دعوت |
| 🎁 **تست رایگان** | یک‌بار: ۱ روزه، ۵۰۰ مگ، پرسرعت‌ترین سرور؛ انقضای خودکار |
| 🔗 **ساب چندکانفیگه** | ~۱۰ سرور در هر ساب + کرون سلامت سرورها و جایگزینی خودکار سرور مرده |
| 🔳 **QR برند** | سرمه‌ای/طلایی + لوگوی سکه وسط + فریم — PNG کاملاً سرورلس |
| 🎮 **مینی‌اپ سکه‌ای** | تپ با انیمیشن، سطوح Bronze/Silver/Gold/Diamond، آپگرید، چرخ شانس روزانه، لیگ هفتگی با جایزه، ماموریت روزانه، فروشگاه سکه‌ای |
| 🤖 **چت هوش مصنوعی** | هوش مصنوعی بومی کلادفلر (**Workers AI**، بدون کلید خارجی) با زنجیرهٔ چند مدلی و fallback — ۵ سکه هر پیام، تاریخچه کوتاه، در خطا سکه کسر نمی‌شود |
| 📡 **تحویل MTProto** | لینک مستقیم `tg://proxy` / `t.me/proxy` با secret واقعی و دکمهٔ «🔗 اتصال مستقیم در تلگرام» |
| 🧑‍💼 **پشتیبانی دوحالته** | انتخاب بین «🤖 دستیار هوشمند» و «🧑‍💼 اپراتور انسانی»؛ در همه پیام‌های AI دکمه اتصال به اپراتور هست |
| 💬 **AI داخل گروه** | پاسخ هوش مصنوعی به ریپلای/منشن بات یا پیام‌های «ربات ...» و `/ai` در گروه، با کول‌داون ضدهرزنامه |
| 📖 **راهنما** | `/help` با شرح کامل همه بخش‌ها و دستورها + دکمه «📖 راهنما» در منوی اصلی |
| 🖥 **پنل تحت وب** | `/panel` با ورود اجباری رمز **دقیقاً ۱۰ رقمی** + مدیریت کامل گروه‌ها (افزودن/حذف/تغییر نام/فعال‌سازی/پست فوری/پیام گروهی/تنظیمات) |
| 📢 **گروه‌ها** | ثبت خودکار، خوش‌آمد عضو جدید، پست تبلیغی زمان‌بندی‌شده با نرخ لحظه‌ای و دکمه «🛒 خرید در پیوی»، پست فوری دستی، مدیریت کامل از پنل تلگرام و پنل وب |
| ⏰ **یادآوری** | قبل از انقضا + تمدید سریع + تخفیف کاربر غیرفعال |
| 💵 **نرخ تتر واقعی** | میانهٔ قیمت USDT از صرافی‌های داخلی (نوبیتکس/والکس/بیت‌پین/رمزینکس) + کش + رد مقادیر پرت + هشدار انحراف — بدون عدد جعلی |
| 🧮 **قیمت‌گذاری پویا** | فرمول کاملاً قابل تنظیم از پنل + تخفیف پلکانی حجم/مدت + تخفیف وفاداری + کد تخفیف (درصدی/مبلغی، سقف استفاده، انقضا، حداقل سفارش) |
| 🎛 **کانفیگ‌ساز پویا** | پیش‌نمایش قیمت لحظه‌ای در پنل وب (پروتکل/مدت/حجم/دستگاه/کیفیت/لوکیشن) |
| 🌐 **مخزن IP تمیز** | سنجش واقعی از داخل ایران از طریق پروب مینی‌اپ کاربران + امتیازدهی + ضدتقلب + مدارشکن + ایمپورت رنج کلادفلر/URL/دستی |
| 🎲 **ساخت خودکار سکرت** | UUID / پسورد امن / سکرت MTProto (خام/dd/ee+FakeTLS) + راهنمای کلید Reality |

---

## 🛡 چرا «تضمین ۱۰۰٪» ممکن نیست و ما به‌جایش چه کردیم

هیچ سرویسی نمی‌تواند اتصال همیشگی روی شبکه‌ای که بخش‌هایی از آن در حال مسدودسازی و
تغییر مداوم است را **تضمین** کند — نه ما و نه هیچ‌کس دیگر. ادعای تضمین، فقط دروغ است.
به‌جای یک وعدهٔ غیرقابل‌اجرا، معماری را «چندلایه و همیشه‌روشن» چیده‌ایم تا وقتی یک
مسیر مرد، مسیر بعدی خودش کار را بگیرد و کاربر فقط ساب را رفرش کند:

| # | لایه | وضعیت در این ریپو |
|---|---|---|
| ۱ | **چند ترنسپورت هم‌زمان** (WebSocket+TLS → gRPC → XHTTP/SplitHTTP → HTTPUpgrade → Reality) | زیرساخت قالب‌ها و ستون‌های سرور آماده است؛ قالب‌های کامل چند-ترنسپورتی هنوز پیاده‌سازی نشده‌اند |
| ۲ | **چند IP تمیز برای هر کانفیگ** مرتب‌شده بر اساس امتیاز واقعی | ✅ مخزن `clean_ips` + پروب + امتیازدهی پیاده شد؛ خروجی ساب هنوز چند-IP در هر کانفیگ نمی‌دهد |
| ۳ | **ساب پویا** — هر fetch بر اساس امتیاز لحظه‌ای و سلامت سرورها | ⏳ ساخت خروجی با ترتیب امتیاز آماده است؛ اتصال کامل آن به `/sub` باقی مانده |
| ۴ | **خود-ترمیمی در کرون** | ✅ سلامت سرورها + جایگزینی خودکار (موجود) + مدارشکن و بازگردانی IPهای بد (جدید) |
| ۵ | **Circuit Breaker** | ✅ برای IPهای تمیز (۵ شکست پیاپی → خروج موقت ۱ ساعته + بازگشت خودکار) |
| ۶ | **Kill-switch محصول** | ⏳ هنوز پیاده نشده (توقف خودکار فروش وقتی هیچ مسیر سالمی برای یک پروتکل نمانده) |

> ⚠️ **صادقانه:** اگر **همهٔ** لایه‌ها هم‌زمان بمیرند (مثلاً کل رنج IP مسدود شود و همهٔ
> سرورها قطع شوند)، هیچ لایه‌ای نمی‌تواند اتصال را برگرداند — در آن حالت سیستم باید
> صادقانه «سرویس در دسترس نیست» بگوید و تحویل کانفیگ مرده را متوقف کند (Kill-switch)،
> نه اینکه کانفیگ خراب تحویل دهد. این رفتار «نگفتن دروغ» در کل ربات رعایت شده است.

---

## 🌐 مخزن IP تمیز و پروب واقعی از داخل ایران

**نکتهٔ کلیدی:** Worker داخل دیتاسنتر خارج از ایران اجرا می‌شود، پس هر تستی که خود
Worker انجام دهد هیچ ربطی به کیفیت اتصال از نت ملی ندارد و ICMP/ping روی Workers
وجود ندارد. تنها جایی که کد ما داخل ایران اجرا می‌شود، **مینی‌اپ تلگرام روی گوشی کاربر**
است. بنابراین:

- `GET /api/probe/targets` → چند IP کاندید (چرخشی، وزن‌دار)
- کاربر در مینی‌اپ با درخواست کوچک و timeout کوتاه، زمان رسیدن به هر IP را می‌سنجد
- `POST /api/probe/report` با `initData` معتبر → ثبت نتیجه + امتیاز + سکهٔ انگیزشی
- امتیازدهی: میانگین متحرک نمایی + نرخ موفقیت + جریمهٔ شکست پیاپی + وزن تعداد نمونه
- ضدتقلب: سقف روزانه، تشخیص گزارش تکراری، حذف مقادیر پرت با IQR، حداقل نمونه
- تفکیک اپراتور (همراه اول/ایرانسل/مخابرات/رایتل) از گزارش کاربر گرفته می‌شود

> ⚠️ **محدودیت دقیق پروب:** پروب مرورگر/وب‌اپ به‌خاطر CORS معمولاً نمی‌تواند به هر
> دامنه‌ای درخواست واقعی بزند و فقط «تاخیر رسیدن به آن IP از شبکهٔ اپراتور کاربر»
> را تقریب می‌زند — نه توان عملیاتی کامل کانفیگ را. امتیازها صرفاً یک سیگنال نسبی
> برای رتبه‌بندی IPها هستند. کل این قابلیت با کلید `probe_enabled` (پیش‌فرض خاموش)
> توسط ادمین فعال می‌شود.

---

## 💵 نرخ تتر — چرا قبلاً خراب بود و الان چه شد

- `open.er-api.com` فقط نرخ **رسمی/دولتی** IRR می‌دهد (نه بازار آزاد) و
  `api.frankfurter.dev` اصلاً IRR ندارد؛ نتیجه fallback ثابت `100000` و قیمت‌های غلط بود.
- اکنون: قیمت تتر از **نوبیتکس، والکس، بیت‌پین، رمزینکس** گرفته می‌شود و **میانه** چند
  منبع استفاده می‌شود (نه اولین موفق). انحراف >۲۰٪ نسبت به کش، هشدار ثبت می‌کند.
- اگر همهٔ منابع fail شوند: آخرین کش؛ اگر کش هم نبود **عدد جعلی برنمی‌گردد** و پنل
  پیام «نرخ آنلاین در دسترس نیست، نرخ دستی بگذارید» نشان می‌دهد.

> ⚠️ **صادقانه:** این endpointها از دیتاسنتر Cloudflare صدا زده می‌شوند و ممکن است برخی
> صرافی‌های ایرانی درخواست از IP دیتاسنتر را بلاک کنند. تایید نهایی فقط بعد از دیپلوی
> روی حساب واقعی Cloudflare و با دکمهٔ «🔄 بروزرسانی فوری» در پنل ممکن است. جایگزین:
> تنظیم نرخ دستی از پنل.

---

## ⚙️ تنظیم توکن و آدرس Worker

- در `wrangler.toml` هیچ `[vars]`ای برای توکن یا URL وجود ندارد؛ Deploy to Cloudflare به‌خاطر این پروژه فیلد متغیر ربات نشان نمی‌دهد.
- مسیر `/setup` توکن را از فرم می‌گیرد، با `getMe` اعتبارسنجی می‌کند، `setWebhook` را صدا می‌زند و سپس مقدار را در جدول `settings` داخل Durable Object ذخیره می‌کند.
- کد در هر درخواست ابتدا `telegram_bot_token` را از Durable Object می‌خواند و فقط اگر وجود نداشت از `env.TELEGRAM_BOT_TOKEN` استفاده می‌کند. متغیر env برای `.dev.vars` در توسعه محلی و نصب‌های قدیمی نگه داشته شده است.
- origin عمومی از اولین درخواست کش می‌شود و نیازی به تنظیم URL دستی نیست.

> 🤖 **هوش مصنوعی هیچ کلید خارجی نمی‌خواهد.** همه‌چیز روی **Workers AI** بومی کلادفلر اجرا می‌شود
> (بایندینگ `[ai]` در `wrangler.toml` — با دیپلوی خودکار فعال می‌شود).
> مدل متن: `@cf/meta/llama-3.1-8b-instruct` | مدل بینایی: `@cf/meta/llama-3.2-11b-vision-instruct`

## 🖥 پنل تحت وب

آدرس: `https://aminck-nova-bot.<زیردامنه-تو>.workers.dev/panel`

- ورود فقط با رمز **دقیقاً ۱۰ رقم عددی** (هم در سمت مرورگر و هم سمت سرور اجبار می‌شود).
- اگر رمزی تنظیم نشده باشد، در اولین اجرا یک رمز ۱۰ رقمی تصادفی ساخته و ذخیره می‌شود.
- دیدن/تغییر رمز از تلگرام: **پنل مدیریت → ⚙️ تنظیمات → 🔐 رمز پنل وب (۱۰ رقم)**
- **همهٔ** مسیرهای `/api/panel/*` (به‌جز `login`) بدون سشن معتبر پاسخ `401` می‌دهند؛ کاربر عادی به هیچ داده‌ای دسترسی ندارد.
- توکن ربات و رمز پنل هرگز در HTML یا خروجی API ظاهر نمی‌شوند و از API تنظیمات هم قابل تغییر نیستند.

### تب‌های پنل

| تب | امکانات |
|---|---|
| 📊 داشبورد | فروش امروز/۷ روز/۳۰ روز/کل، کاربران (کل، جدید، فعال، بن)، سفارش‌ها، فیش‌های در صف، اشتراک فعال و نزدیک‌انقضا، سرور آمادهٔ تحویل، مجموع سکه و کیف پول + **هشدار نبود سرور واقعی** |
| 📦 محصولات | افزودن/ویرایش/فعال‌سازی/حذف (با تایید)، جست‌وجو و صفحه‌بندی |
| 🖥 سرورها | ثبت کامل **نام، کشور، پروتکل، هاست/IP، پورت، secret/UUID/پسورد، قالب کانفیگ، آدرس تست سلامت، رتبهٔ سرعت**؛ نمایش مشکلات پیکربندی، تست سلامت تک‌سرور، فعال‌سازی فقط با پیکربندی کامل |
| 👥 کاربران | جست‌وجو با نام/یوزرنیم/آیدی، جزئیات (اشتراک‌ها و سفارش‌ها)، شارژ/کسر کیف پول و سکه، ارسال پیام مستقیم، بن/رفع بن |
| 🧾 سفارش‌ها | فیلتر بر اساس وضعیت + صفحه‌بندی |
| 💳 فیش‌ها | تایید (با تحویل خودکار) یا رد، همراه گزارش هوش مصنوعی و پیام خطای دقیق |
| 📢 گروه‌ها | افزودن/حذف/تغییر نام/فعال‌سازی، پست فوری، پیام همگانی |
| ⚙️ تنظیمات | AI (فعال‌سازی، انتخاب مدل Workers AI، هزینهٔ سکه، **تست اتصال**)، قیمت‌گذاری، کلیدهای فعال/غیرفعال، تبلیغ گروه، ویرایش متن‌های بات، تغییر رمز پنل |

طراحی موبایل‌فرست و کاملاً **RTL فارسی**، با حالت‌های بارگذاری/خطا/موفقیت، تایید قبل از عملیات مخرب و صفحه‌بندی روی همهٔ کوئری‌های سنگین.

---

## 🖧 سرورها و تحویل واقعی (بدون کانفیگ نمونه)

از این نسخه به بعد **هیچ کانفیگ نمونه/تصادفی تحویل داده نمی‌شود**:

- سرورهای نمونهٔ `*.example.com` دیگر فعال نیستند و یک **مهاجرت خودکار** (`migration:placeholder_servers_v1`)
  روی نصب‌های قدیمی هم آن‌ها را غیرفعال می‌کند — تغییر seed به‌تنهایی کافی نبود چون فلگ `seeded` از قبل ثبت شده بود.
- اگر هیچ سرور واقعیِ فعالی وجود نداشته باشد، خرید/تحویل با پیام
  «⚠️ هنوز سرور واقعی تنظیم نشده است؛ این محصول فعلاً قابل تحویل نیست.» متوقف می‌شود و **پول کاربر کسر نمی‌شود**.
- صفحهٔ `/sub` هیچ کانفیگ `example.com` یا سرور ناقصی نشان نمی‌دهد.

### افزودن سرور واقعی

از تلگرام: **پنل مدیریت → 🖥 سرورها → ➕ سرور جدید**
(نام → پروتکل → هاست/IP → پورت → secret → قالب → آدرس سلامت → رتبهٔ سرعت)

یا از پنل وب: تب **🖥 سرورها → ➕ سرور جدید**.

> 🩺 **دربارهٔ تست سلامت:** این تست فقط یک درخواست HTTP از شبکهٔ Cloudflare است.
> Workers اجازهٔ ICMP/ping ندارد، بنابراین نتیجهٔ آن **معادل پینگ واقعی از داخل ایران نیست**
> و هیچ ادعایی دربارهٔ کیفیت اتصال از نت ملی نمی‌کند.

---

## 📡 تحویل MTProto (لینک مستقیم تلگرام)

محصولات MTProto و SOCKS5 دیگر لینک `/sub/<token>` نمی‌گیرند؛ کاربر یک **لینک اتصال مستقیم**
با دکمهٔ «🔗 اتصال مستقیم در تلگرام» دریافت می‌کند:

```
tg://proxy?server=<host>&port=<port>&secret=<secret>
https://t.me/proxy?server=<host>&port=<port>&secret=<secret>
```

- `secret` همان مقدار **ذخیره‌شده در دیتابیس** است؛ هرگز از UUID تصادفی ساخته نمی‌شود.
- سکرت‌های پذیرفته‌شده: ۳۲ کاراکتر هگز، یا `dd`/`ee` + هگز، یا base64url (حداقل ۲۲ کاراکتر).
- اگر host/port/secret ناقص باشد **هیچ لینکی ساخته نمی‌شود**: کاربر پیام «به پشتیبانی ارجاع شد» می‌گیرد
  و ادمین‌ها گزارش دقیق مشکل پیکربندی را دریافت می‌کنند.
- VLESS / VMess / Trojan / Shadowsocks همچنان لینک اشتراک `/sub/<token>` می‌گیرند.

---

## 🤖 هوش مصنوعی (Workers AI)

**ریشهٔ خرابی قبلی:** مدل `@cf/meta/llama-3.1-8b-instruct` توسط Cloudflare **منسوخ (Deprecated)** شده است،
بنابراین هر فراخوانی شکست می‌خورد و کاربر پیام «دستیار هوشمند فعلاً در دسترس نیست» می‌دید.

اکنون یک **زنجیرهٔ مدل** با fallback استفاده می‌شود:

1. `@cf/meta/llama-3.3-70b-instruct-fp8-fast`
2. `@cf/meta/llama-3.1-8b-instruct-fp8`
3. `@cf/meta/llama-3.2-3b-instruct`

- ادمین می‌تواند از **پنل وب → ⚙️ تنظیمات → مدل Workers AI** مدل دلخواه را انتخاب کند؛ اگر مدلِ انتخابی منسوخ باشد نادیده گرفته می‌شود.
- خطاها تفکیک می‌شوند: `binding_missing` / `model_error` / `quota_exceeded` / `invalid_response` / `disabled`.
- همهٔ شکل‌های پاسخ پارس می‌شوند: `response`، `result.response`، `choices[].message.content` و رشتهٔ خام.
- **در هیچ خطایی سکه کسر نمی‌شود** و به کاربر دکمه‌های «🔁 تلاش دوباره» و «🧑‍💼 اتصال به اپراتور» داده می‌شود.
- دکمهٔ **🧪 تست اتصال هوش مصنوعی** در پنل وب وضعیت binding، مدل پاسخ‌گو، زمان پاسخ و کد خطا را نشان می‌دهد (بدون افشای هیچ توکنی).

---

## 🎡 گردونهٔ شانس

- `/api/game/spin` با یک **قفل اتمیک** روی ستون `users.spin_claim` کار می‌کند؛
  در چند درخواست هم‌زمان (دابل‌کلیک) فقط یکی برنده می‌شود.
- پاسخ موفق: `{ ok, prize, index, segments, coins, spinDone }` — `index` شمارهٔ برشِ برنده روی گردونه است.
- پاسخ تکراری: `{ ok:false, reason:'already', spinDone:true, coins }`.
- فرانت‌اند: اشاره‌گر ثابت، چرخش رو‌به‌جلو تا مرکز برشِ برنده، پایان با `transitionend`
  (به‌علاوهٔ تایم‌اوت ایمنی ۴٫۶ ثانیه‌ای)، و در خطا دکمه دوباره فعال می‌شود — بدون قفل‌شدن دائمی.

---

## 🧪 تست

```bash
npm test                    # ۳۱۴ تست روی SQLite واقعی + تلگرام شبیه‌سازی‌شده
npx wrangler deploy --dry-run
```

تست‌ها اقتصاد سکه، شارژ کیف پول، پاداش رفرال، ویزاردهای ادمین، احراز هویت و امنیت پنل وب،
رمز ۱۰ رقمی، چت AI در گروه و امنیت مینی‌اپ را پوشش می‌دهند؛ به‌علاوه:

- **۲۱)** ساخت لینک مستقیم MTProto با سکرت ذخیره‌شده + رد کردن هاست نمونه و سکرت جعلی + رگرسیون لینک `/sub` برای VLESS
- **۲۲)** نبودِ `example.com` در خروجی اشتراک و صفحهٔ `/sub` + مهاجرت غیرفعال‌سازی سرورهای نمونه
- **۲۳)** توقف خرید و تحویل وقتی سرور واقعی وجود ندارد (بدون کسر پول)
- **۲۴)** گردونه: چرخش موفق، رد چرخش تکراری، رد چهار درخواست هم‌زمان به‌جز یکی، تطابق `index` با جایزه
- **۲۵)** AI: منسوخ‌نبودن مدل پیش‌فرض، موفقیت، نبود binding، خطای مدل، خطای سهمیه، پاسخ نامعتبر، چهار شکل مختلف پاسخ، عدم کسر سکه
- **۲۶)** ارسال دقیقاً یک لینک اشتراک و جلوگیری از تحویل تکراری
- **۲۷)** امنیت پنل وب: ۴۰۱ برای همهٔ APIها بدون سشن، عدم افشای توکن/رمز/سکرت، مدیریت محصول و سرور، اعتبارسنجی
- **۲۸)** قیمت‌گذاری پویا: فرمول، تخفیف پلکانی، تخفیف وفاداری، کد تخفیف (درصدی/مبلغی/انقضا/سقف/حداقل سفارش)، مصرف کد و رند کردن
- **۲۹)** مخزن IP تمیز: تجزیهٔ ورودی، نمونه‌برداری CIDR، امتیازدهی، ثبت گزارش با initData معتبر، سقف روزانه، مدارشکن شکست پیاپی، ایمپورت رنج کلادفلر، اعمال روی سرورها
- **۳۰)** ساخت خودکار سکرت: UUID، پسورد امن، سکرت MTProto (خام/dd/ee+FakeTLS) با اعتبارسنجی `isValidMtprotoSecret`، راهنمای Reality
- **۱۳)** نرخ ارز (بازنویسی‌شده): میانهٔ چند صرافی داخلی، رد پرت، هشدار انحراف، کش کهنه در شکست همه، عدد جعلی در نبود کش

> ⚠️ **صادقانه:** این تست‌ها همگی محلی و با تلگرام و Workers AI شبیه‌سازی‌شده اجرا می‌شوند.
> پاسخ واقعی مدل‌های Workers AI و تحویل واقعی پیام در تلگرام تنها پس از دیپلوی روی حساب Cloudflare
> و از طریق دکمهٔ «🧪 تست اتصال هوش مصنوعی» در پنل قابل تایید است.

## 🕐 کرون‌جاب

هر ۱۵ دقیقه: کش نرخ ارز (رفرش کامل هر ۴ ساعت)، تست سلامت سرورها (هر ۶ ساعت) + جایگزینی خودکار سرور مرده، یادآوری انقضا، پست تبلیغی گروه‌ها، پرداخت جایزه لیگ هفتگی.

---

## 🗂 ساختار

```
src/
├── index.js      # ورکر، وب‌هوک، خود-راه‌اندازی پروفایل بات، مسیرهای وب
├── setup.js      # صفحه /setup، ذخیره توکن در DB و ثبت setWebhook
├── config.js     # اولویت توکن Durable Object بر env fallback
├── store.js      # Durable Object SQLite + شیم سازگار (بدون KV/D1 دستی)
├── db.js         # اسکیما + سید خودکار + «اولین کاربر = سوپرادمین»
├── tg.js         # کلاینت Bot API + کیبوردها
├── texts.js      # متن‌های فارسی قابل‌ویرایش از پنل
├── user.js       # منو، فروشگاه، حساب، رفرال، تست، پشتیبانی، چت AI
├── admin.js      # پنل مدیریت کامل با سطوح دسترسی
├── pay.js        # سفارش/کیف‌پول/کارت/تحویل/پاداش رفرال
├── ai.js         # لایه Workers AI (چت متنی + بینایی) — بدون کلید خارجی
├── panel.js      # پنل تحت وب کامل (داشبورد/محصولات/سرورها/کاربران/سفارش‌ها/فیش‌ها/گروه‌ها/تنظیمات)
├── proxy.js      # اعتبارسنجی سرور + ساخت لینک مستقیم MTProto/SOCKS5 تلگرام
├── verify.js     # بررسی فیش با بینایی Workers AI
├── subs.js       # ساب چندکانفیگه + جایگزینی سرور مرده + لندینگ
├── qr.js         # QR برند (انکودر PNG سرورلس)
├── game.js       # API و صفحه مینی‌اپ سکه‌ای (WebApp)
├── group.js      # خوش‌آمد گروه + پست تبلیغ زمان‌بندی‌شده + چت AI داخل گروه
├── jobs.js       # کرون‌جاب‌ها (شامل نگهداری مخزن IP تمیز)
├── pricing.js    # نرخ لحظه‌ای تتر از صرافی‌های داخلی + موتور قیمت‌گذاری پویا + کد تخفیف
├── cleanip.js    # مخزن IP تمیز + پروب + امتیازدهی + ضدتقلب + مدارشکن
├── secrets.js    # ساخت خودکار UUID / پسورد / سکرت MTProto + راهنمای Reality
└── logo.js       # لوگوی بات (بیس۶)
```

---

## 🔐 نکته امنیتی

توکن بات در این ریپازیتوری قرار نمی‌گیرد. در تولید آن را فقط از `/setup` وارد کنید تا داخل Durable Object همان Worker ذخیره شود؛ برای توسعه محلی نیز فقط از فایل ignored به نام `.dev.vars` استفاده کنید. اگر یک نصب قدیمی Secret محیطی داشته باشد، آن مقدار فقط fallback است و مقدار ذخیره‌شده در Durable Object اولویت دارد.

ساخته‌شده با ❤️ — AMINCK
